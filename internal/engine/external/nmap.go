package external

import (
	"context"

	"github.com/Ruohao1/penta/internal/model"
)

func RunNmap(ctx context.Context, req model.Request, emit func(context.Context, model.Event)) error {
	return nil
}
